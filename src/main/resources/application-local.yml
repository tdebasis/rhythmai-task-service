spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/havq-tasks-local

logging:
  level:
    com.havq.task: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.web: DEBUG

management:
  endpoint:
    health:
      show-details: always